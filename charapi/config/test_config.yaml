mock_mode: true

data_fields:
  program_expenses:
    source: manual
  admin_expenses:
    source: manual
  fundraising_expenses:
    source: manual
  in_pub78:
    source: manual
  is_revoked:
    source: manual
  has_recent_filing:
    source: manual
  charity_navigator_rating:
    source: manual
  total_revenue:
    source: api
  total_expenses:
    source: api

manual_data:
  directory: "manual"

propublica:
  base_url: "https://projects.propublica.org/nonprofits/api/v2"
  timeout: 30
  mock_mode: false

irs:
  local_data_dir: "cache"
  mock_mode: false

scoring:
  financial:
    program_expense_target: 0.75
    admin_expense_limit: 0.15
    fundraising_expense_limit: 0.15
    program_score_max: 40
    admin_score_max: 20
    fundraising_score_max: 20
    stability_score_max: 20
  
  trend:
    years_to_analyze: 5
    growth_score_range: 10
    volatility_penalty_range: 10

caching:
  enabled: false
  database_path: "cache/test_cache.db"
  default_ttl_hours: 1
  propublica_ttl_hours: 1
  charity_navigator_ttl_hours: 1
  cleanup_on_startup: false